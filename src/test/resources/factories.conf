user {
  userId="#{userId}"
  name="user#{userId}"
  email="user#{userId}@example.com"
  imageUrl="http://s3.example.com/images/user#{userId}.png"
  comment="user#{userId}'s comment"
  locale="en"
  isActive=1
}
article {
  articleId="#{articleId}"
  userId=1
  title="article title#{articleId}"
  body="article body#{articleId}"
  commentsCount=0
  stocksCount=0
  likesCount=0
}
tag {
  tagId="#{tagId}"
  name="tag#{tagId}"
  taggingsCount=1
}
comment {
  commentId="#{commentId}"
  userId="#{userId}"
  articleId="#{articleId}"
  body="comment body#{commentId}"
}
stock {
  stockId="#{stockId}"
  userId="#{userId}"
  articleId="#{articleId}"
}
like {
  likeId="#{likeId}"
  userId="#{userId}"
  articleId="#{articleId}"
}
notification {
  notificationId="#{notificationId}"
  userId="#{userId}"
  articleId="#{articleId}"
  fragmentId=1
  senderId="#{senderId}"
  type="comment"
  state=0
}